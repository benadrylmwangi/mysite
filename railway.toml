[build]
builder = "NIXPACKS"
buildCommand = "python manage.py collectstatic --noinput"

[deploy]
startCommand = "gunicorn mysite.wsgi:application --bind 0.0.0.0:$PORT --log-file -"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
